CREATE DATABASE `jd2_homework3-4`;

USE `jd2_homework3-4`;

CREATE TABLE film (
id INT AUTO_INCREMENT PRIMARY KEY,
name_film VARCHAR(50) NOT NULL,
duration VARCHAR(50) NOT NULL,
price INT NOT NULL
);

INSERT INTO film (name_film, duration, price) VALUES ('Matrix', 90, 50);
INSERT INTO film (name_film, duration, price) VALUES ('Shrek', 60, 100);
INSERT INTO film (name_film, duration, price) VALUES ('Pirates of the Caribbean', 120, 150);
INSERT INTO film (name_film, duration, price) VALUES ('Forrest Gump', 120, 100);
INSERT INTO film (name_film, duration, price) VALUES ('The Lion King', 90, 100);

SELECT max(price) FROM film;

DELETE FROM film WHERE price = (SELECT * FROM (SELECT min(price) FROM film) AS t1);

UPDATE film SET name_film = “Hot film!” WHERE price = (SELECT * FROM (SELECT max(price) FROM film) AS t1);

DROP TABLE film;

DROP DATABASE `jd2_homework3-4`;